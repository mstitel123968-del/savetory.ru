version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env.prod
    depends_on:
      - db
    volumes:
      - static_data:/app/staticfiles
      - media_data:/app/media

  db:
    image: postgres:16.3
    restart: unless-stopped
    env_file:
      - .env.prod
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    # SECURITY: do NOT publish the database port to the host. The web service
    # reaches Postgres over the internal Docker network as "db:5432". Publishing
    # 5432 exposes the database to the public internet. For trusted local
    # debugging only, bind to loopback instead, e.g.:
    #   ports:
    #     - "127.0.0.1:5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

  nginx:
    image: nginx:1.27
    restart: unless-stopped
    depends_on:
      - web
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /opt/savetory/letsencrypt:/etc/letsencrypt:ro
      - /opt/savetory/certbot/www:/var/www/certbot:ro
      - static_data:/var/www/static:ro
      - media_data:/var/www/media:ro

volumes:
  pg_data:
  static_data:
  media_data:
